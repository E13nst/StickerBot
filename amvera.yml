---
version: 1
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11
build:
  commands:
    - pip install -r requirements.txt
  useCache: true

run:
  command: python main.py
  persistenceMount: /data
  containerPort: 80
