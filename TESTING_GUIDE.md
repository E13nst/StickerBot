# Руководство по тестированию обработки стикеров

## Обзор реализованных функций

Реализована улучшенная обработка стикеров с:
- ✅ Асинхронным кэшем проверок наличия стикерсетов (TTL: 7 дней, размер: 5000)
- ✅ Разным поведением для персональных и групповых чатов
- ✅ Реакциями ✅ на существующие стикерсеты (во всех типах чатов)
- ✅ Реакциями 👍 после успешного добавления (во всех типах чатов)
- ✅ Graceful degradation при ошибках
- ✅ Метриками для мониторинга

## Автоматические тесты

### Запуск тестов кэша

```bash
cd /Users/andrey/PycharmProjects/StickerBot
source venv/bin/activate
python -m pytest tests/test_utils/test_stickerset_cache.py -v
```

**Результат**: ✅ Все 12 тестов прошли успешно

Тесты покрывают:
- Инициализацию кэша
- Set/Get операции
- Cache hits/misses
- TTL expiration
- LRU eviction
- Обновление записей
- Инвалидацию
- Статистику
- Конкурентный доступ

## Мануальное тестирование в Telegram

### Подготовка

1. Запустите бота:
```bash
source venv/bin/activate
python main.py
```

2. Убедитесь, что в логах появилось:
```
StickerSet cache initialized
Sticker set cache cleanup task started
```

### Тест 1: Персональный чат - Существующий стикерсет

**Шаги:**
1. Откройте персональный чат с ботом
2. Отправьте стикер из популярного набора (который уже в галерее)

**Ожидаемый результат:**
- ✅ На стикере появляется реакция ✅
- ✅ Бот отправляет текстовое сообщение: "Мы уже знаем этот стикерсет — он уже в Галерее 🔁..."
- ✅ Показывается кнопка "Посмотреть в Stixly"
- ✅ В логах: `Cache HIT for https://t.me/addstickers/...` (при повторной отправке)

**Проверка кэша:**
- Отправьте тот же стикер еще раз
- В логах должен быть `Cache HIT` вместо `Cache MISS`

### Тест 2: Персональный чат - Новый стикерсет

**Шаги:**
1. Откройте персональный чат с ботом
2. Отправьте стикер из редкого набора (которого нет в галерее)

**Ожидаемый результат:**
- ✅ Бот отправляет: "О! Такого я ещё не видел 👀..."
- ✅ Показывается кнопка "Добавить в галерею"
- ✅ В логах: `Cache MISS for https://t.me/addstickers/...`

### Тест 3: Персональный чат - Добавление в галерею

**Шаги:**
1. Продолжите с теста 2
2. Нажмите кнопку "Добавить в галерею"

**Ожидаемый результат:**
- ✅ Бот отправляет: "✅ Стикерсет успешно добавлен в галерею!"
- ✅ На исходном сообщении со стикером появляется реакция 👍
- ✅ В логах: `Successfully added reaction 👍 to message ...`
- ✅ В логах: `Cache saved: ...`

### Тест 4: Групповой чат - Существующий стикерсет

**Шаги:**
1. Добавьте бота в группу/супергруппу
2. Отправьте стикер из популярного набора (который уже в галерее)

**Ожидаемый результат:**
- ✅ Бот ставит реакцию ✅ на сообщение со стикером
- ✅ Бот НЕ отправляет текстовое сообщение
- ✅ В логах: `Set reaction ✅ on message ...`
- ✅ В логах: `Cache HIT for ...` (при повторной отправке)

### Тест 5: Групповой чат - Новый стикерсет

**Шаги:**
1. В той же группе отправьте стикер из редкого набора

**Ожидаемый результат:**
- ✅ Бот отправляет текстовое сообщение: "О! Такого я ещё не видел 👀..."
- ✅ Показывается кнопка "Добавить в галерею"
- ✅ В логах: `Cache MISS for ...`

### Тест 6: Групповой чат - Добавление в галерею

**Шаги:**
1. Продолжите с теста 5
2. Нажмите кнопку "Добавить в галерею"

**Ожидаемый результат:**
- ✅ Бот отправляет: "✅ Стикерсет успешно добавлен в галерею!"
- ✅ На исходном сообщении со стикером появляется реакция 👍
- ✅ В логах: `Successfully added reaction 👍 to message ...`

### Тест 7: Graceful Degradation - Реакции недоступны

**Шаги:**
1. В группе, где реакции отключены, отправьте существующий стикер

**Ожидаемый результат:**
- ✅ В логах: `Failed to set reaction ✅: ...`
- ✅ Бот продолжает работать (не падает)
- ✅ Никаких текстовых сообщений не отправляется

### Тест 8: Graceful Degradation - Ошибка кэша

**Шаги:**
1. Смоделируйте сбой кэша (временно измените код)
2. Отправьте любой стикер

**Ожидаемый результат:**
- ✅ В логах: `Cache error, falling back to API: ...`
- ✅ Бот продолжает работать через Gallery API
- ✅ Функционал работает корректно

### Тест 9: Метрики кэша

**Шаги:**
1. Подождите ~1 час (интервал фоновой очистки)

**Ожидаемый результат в логах:**
```
Cache stats: size=X/5000, hits=Y, misses=Z, hit_rate=A%, evictions=B, expired_removed=C
```

**Проверка:**
- ✅ `hit_rate` должен быть > 50% при активном использовании
- ✅ `size` не превышает 5000
- ✅ `evictions` увеличивается при достижении лимита

### Тест 10: TTL кэша

**Шаги:**
1. Отправьте стикер (записывается в кэш)
2. Подождите 7 дней
3. Отправьте тот же стикер

**Ожидаемый результат:**
- ✅ После 7 дней: `Cache MISS` (запись устарела)
- ✅ В логах: `Cache entry expired for ..., age=...`

## Мониторинг производительности

### Проверка эффективности кэша

**Метрики для мониторинга:**

1. **Hit Rate** (должен быть 60-80%):
   ```
   hit_rate = hits / (hits + misses)
   ```

2. **Latency**:
   - Cache HIT: < 1ms
   - Cache MISS → API: ~200ms

3. **Memory Usage**:
   - Ожидаемый размер: ~500 KB (5000 записей × ~100 байт)

4. **Evictions**:
   - Если > 100/час → увеличить `STICKERSET_CACHE_SIZE`

### Настройка через переменные окружения

Добавьте в `.env`:

```bash
# Размер кэша (по умолчанию: 5000)
STICKERSET_CACHE_SIZE=5000

# TTL в днях (по умолчанию: 7)
STICKERSET_CACHE_TTL_DAYS=7

# Интервал очистки в часах (по умолчанию: 1)
STICKERSET_CACHE_CLEANUP_INTERVAL_HOURS=1
```

## Troubleshooting

### Проблема: Реакции не работают в группах

**Причины:**
1. Реакции отключены в настройках группы
2. Бот не имеет прав на установку реакций
3. Старая версия Telegram у участников

**Решение:**
- Это ожидаемое поведение
- Бот логирует warning и продолжает работать
- Проверьте логи: `Failed to set reaction`

### Проблема: Низкий hit_rate кэша

**Возможные причины:**
1. Мало повторных запросов одних и тех же стикерсетов
2. TTL слишком короткий
3. Размер кэша слишком мал

**Решение:**
- Увеличьте `STICKERSET_CACHE_TTL_DAYS` до 14
- Увеличьте `STICKERSET_CACHE_SIZE` до 10000

### Проблема: Высокий memory usage

**Решение:**
- Уменьшите `STICKERSET_CACHE_SIZE`
- Уменьшите `STICKERSET_CACHE_TTL_DAYS`
- Фоновая очистка автоматически удалит устаревшие записи

## Логи для отладки

### Успешная работа кэша

```
[INFO] AsyncStickerSetCache initialized: max_size=5000, ttl_days=7, cleanup_interval_hours=1
[INFO] Sticker set cache cleanup task started
[INFO] Cache MISS for https://t.me/addstickers/example, calling Gallery API
[DEBUG] Cache saved: https://t.me/addstickers/example
[INFO] Cache HIT for https://t.me/addstickers/example
[INFO] Set reaction ✅ on message 12345
[INFO] Successfully added reaction 👍 to message 12346
[INFO] Cache stats: size=42/5000, hits=100, misses=10, hit_rate=0.909, evictions=0, expired_removed=5
```

### Graceful degradation

```
[WARNING] Cache lookup failed: ...
[INFO] Cache MISS for ..., calling Gallery API
[WARNING] Failed to set reaction ✅: ...
[WARNING] Failed to update cache after adding: ...
```

## Завершение

После тестирования проверьте:
- ✅ Все функции работают корректно
- ✅ Кэш эффективно снижает нагрузку на API
- ✅ Реакции работают (или gracefully fail)
- ✅ Метрики логируются правильно
- ✅ Нет memory leaks

**Статус реализации**: ✅ Готово к production

