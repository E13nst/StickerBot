# Инструкция по настройке системы поддержки

## Быстрый старт

### 1. Получение Chat ID группы поддержки

#### Способ 1: Через скрипт (рекомендуется)

```bash
# Убедитесь, что бот добавлен в группу и отправьте сообщение в группу
python scripts/get_chat_id_simple.py
```

Скрипт покажет все найденные группы и их Chat ID.

#### Способ 2: Через бота @userinfobot

1. Добавьте @userinfobot в группу поддержки
2. Перешлите любое сообщение из группы боту @userinfobot
3. Он покажет chat_id группы

#### Способ 3: Через логи бота

1. Запустите бота: `python main.py`
2. Отправьте сообщение в группу
3. В логах будет виден chat_id

### 2. Настройка .env файла

Добавьте в `.env`:

```env
# Поддержка
SUPPORT_CHAT_ID=-1001234567890  # Замените на реальный ID вашей группы
SUPPORT_ENABLED=true
SUPPORT_USE_TOPICS=true
```

**Важно:** Chat ID группы всегда отрицательный (начинается с `-100`)

### 3. Подготовка группы поддержки

1. **Создайте группу** в Telegram
2. **Включите Topics:**
   - Настройки группы → Topics → Включить
3. **Добавьте бота как администратора:**
   - Настройки → Administrators → Add Administrator
   - Выберите вашего бота
   - Предоставьте права:
     - ✅ Manage Topics (Управление темами) - **ОБЯЗАТЕЛЬНО**
     - ✅ Send Messages (Отправка сообщений)
     - ✅ Read Messages (Чтение сообщений)

### 4. Запуск и тестирование

```bash
python main.py
```

Проверьте логи - должно появиться:
```
INFO - Обработчик ответов поддержки добавлен для чата -1001234567890
```

### 5. Тестирование функционала

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите "📞 Поддержка"
4. Отправьте тестовое сообщение
5. Проверьте группу поддержки - должно появиться в отдельной теме
6. Ответьте (Reply) в группе - ответ должен прийти пользователю

## Устранение проблем

### Ошибка: "Обработчик ответов поддержки не добавлен"

**Причина:** SUPPORT_CHAT_ID не установлен или неверный формат

**Решение:**
- Проверьте `.env` файл
- Убедитесь, что Chat ID отрицательный (начинается с `-`)
- Перезапустите бота

### Ошибка: "Не удалось создать тему"

**Причина:** Topics не включены или у бота нет прав

**Решение:**
- Включите Topics в настройках группы
- Проверьте права бота (Manage Topics)
- Убедитесь, что группа - супергруппа

### Ошибка: "Не удалось определить пользователя для ответа"

**Причина:** Связь сообщений не сохранена

**Решение:**
- Проверьте файл `data/support_mapping.json`
- Убедитесь, что отвечаете на сообщение, пересланное ботом
- Перезапустите бота

## Структура файлов

После работы бота создаются файлы:

```
data/
  ├── support_mapping.json  # Хранилище связей сообщений
  └── bot_config.yaml       # Конфигурация бота
```

Формат `support_mapping.json`:

```json
{
  "mappings": {
    "123": {
      "user_id": 456789,
      "topic_id": 2
    }
  },
  "user_topics": {
    "456789": 2
  }
}
```

## Команды бота

- `/start` - Главное меню
- `/help` - Справка по функционалу
- `/support` - Быстрый вход в режим поддержки
- `/cancel` - Отменить текущее действие

