# StickerBot

## Пользовательский поток

### Старт
- `/start` очищает состояние и показывает главное меню с кнопками:
  - `🔍 Найти стикер в галерее` (WebApp)
  - `🛠 Управление стикерами` (открывает подменю)
  - `📢 Telegram-канал` (ссылка)

В подменю "🛠 Управление стикерами" доступны:
  - `Создать новый стикерсет`
  - `Добавить в существующий`
  - `Управлять публикацией`
  - `Назад в главное меню`

`/cancel` доступна в любой момент: очищает состояние и завершает диалог.

### Создать новый стикерсет
1. **Название** – бот просит и сохраняет отображаемое название.
2. **Первый стикер** – пользователь отправляет файл (PNG/JPG/WebP) или стикер, бот конвертирует в WebP.
3. **Эмодзи** – сопоставляем эмодзи со стикером.
4. **Повторение** – сообщение вида «Стикер добавлен! Теперь в наборе X шт. Хочешь добавить ещё один — просто отправь файл в формате PNG, JPG или WebP. Когда закончишь, нажми кнопку «Готово»».
   - При файле → шаги 2–3.
5. **Готово** – бот просит короткое имя (латиница/цифры/`_`, минимум 3 символа), проверяет доступность,
   создаёт набор (`createNewStickerSet`), докидывает остальные стикеры и сохраняет ссылку.
6. **Финал** – сообщение со ссылкой `https://t.me/addstickers/<name>_by_<bot_username>` и отметкой о сохранении в галерею (если успешно).
7. **Публикация** – бот предлагает опубликовать набор в галерее. Пользователь выбирает «🚀 Опубликовать» или «Оставить приватным».

### Добавить в существующий
1. **Выбор набора** – бот подтягивает наборы автора из галереи:
   - inline-кнопки по 2 в ряд, по 10 на страницу, пагинация `⬅️ Назад / ➡️ Вперед`, `⛔️ Отмена`;
   - названия кликабельны, ведущие на `https://t.me/addstickers/<name>` (используем поле `url`, если есть).
2. **Стикер** – после выбора бот подтверждает ссылкой и просит файл (PNG/JPG/WebP) или стикер.
3. **Эмодзи** – сопоставляем эмодзи со стикером и добавляем через `addStickerToSet`.
4. **Повторение** – сообщение «✅ Стикер успешно добавлен в набор!» с кнопкой «Готово».
   - При файле → шаги 2–3.
5. **Готово** – бот сообщает о завершении и приглашает начать заново `/start`.

### Управлять публикацией
1. **Список наборов** – бот подгружает наборы пользователя (по 10 шт. на страницу) и показывает статус:
   - 🌐 — набор опубликован в галерее;
   - 🔒 — набор приватный.
2. **Выбор** – при нажатии на опубликованный набор бот предлагает «🔕 Снять с публикации» или «⬅️ Вернуться» к списку.
   - Для приватных наборов показывается только «⬅️ Вернуться».
3. **Снятие с публикации** – после успеха отображается подтверждение и список обновляется.
4. **Отмена** – кнопка `⛔️ Отмена` завершает сценарий и очищает состояние.

#### Дополнительно
- Все блокирующие вызовы Telegram API и галереи выполняются в фоне (`asyncio.to_thread`).
- `context.user_data` хранит состояние по пользователю; очищается на `/start`, `/cancel` и при завершении.
- Конвертация изображений происходит внутри `ImageService.convert_to_webp`.
- Линки на наборы кликабельны в HTML-формате, документы и сообщения логируются в `bot.log`.
- Бот поддерживает inline режим для поиска стикеров в галерее и генерации стикеров через AI (WaveSpeed API).
- При отправке стикера в главном меню бот проверяет наличие стикерсета в галерее и предлагает добавить его (+10 ART).
